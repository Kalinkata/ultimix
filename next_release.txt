//TODO: add {composer:condition={options:name=show_buttons;default=1}} to every creation form//TODO: create select package dialog//TODO: create select package field (two editable inputs for package name and version + select package dialog)//TODO: rename create_form.tpl in create_{prefix}_form.tpl (make a form_template setting be defaulted to create_{prefix}_form)//TODO: add meta_* files in each package//TODO: add {composer:condition={options:name=show_buttons;default=1}} to every updating form//TODO: create site_manager package